NOTICE:

The host <%= @alert.host%> has failed the last <%= @alert.trigger%> checks.

<%= User.find(@person.first.to_i).fullname%> has acknowledged this alert.

For further updates, please contact them at <%= User.find(@person.first.to_i).email%>