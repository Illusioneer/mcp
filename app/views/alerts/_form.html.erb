<%= form_for(@alert) do |f| %>
  <% if @alert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alert.errors.count, "error") %> prohibited this alert from being saved:</h2>

      <ul>
      <% @alert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :alarm_id %><br />
    <%= f.number_field :alarm_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :since %><br />
    <%= f.datetime_select :since %>
  </div>
  <div class="field">
    <%= f.label :acknowledged %><br />
    <%= f.text_field :acknowledged %>
  </div>
  <div class="field">
    <%= f.label :metadata %><br />
    <%= f.text_field :metadata %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
