You have a new alert created, monitoring <%@alert.host%> until <%@alert.stopdate%>

This alert when triggered will notify:
  <% @alert.notifiers.each do |user| %>
  <%= User.find(user.first).fullname %>,
  <%end%>
  
  