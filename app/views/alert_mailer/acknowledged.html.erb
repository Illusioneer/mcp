NOTICE:

The host <%= @person.host%> has failed the last <%= @person.trigger%> checks.

<%= User.find(@notified.first.to_i).fullname%> has acknowledged this alert.

For further updates, please contact them at <%= User.find(@notified.first.to_i).email%>