ALERT:

<%= person%>

The host <%= @alert.host%> has failed the last <%= @alert.trigger%> checks.

You can <%= link_to "ACKNOWLEDGE", "http://mcp.hcpprod.com/acknowledge/id=#{id}&second=#{status}"%>  it and notify all other parties that you will be looking into it.

You can <%= link_to "IGNORE", "http://mcp.hcpprod.com/ignore/id=#{id}&second=#{status}"%> it and remove yourself from further alerts.

You can <%= link_to "BLOCK", "http://mcp.hcpprod.com/block/id=#{id}&second=#{status}"%> it and remove yourself from this alert forever.

