ALERT:

<%= User.find(@person.first.to_i).fullname%>,

The host <%= @alert.host%> has failed the last <%= @alert.trigger%> checks.

You can <%= link_to "ACKNOWLEDGE", "http://mcp.hcpprod.com:8080/acknowledge/#{@alert.id}/#{@person.first.to_i}"%>  it and notify all other parties that you will be looking into it.

You can <%= link_to "IGNORE", "http://mcp.hcpprod.com:8080/ignore/#{@alert.id}/#{@person.first.to_i}"%> it and remove yourself from further alerts.

You can <%= link_to "BLOCK", "http://mcp.hcpprod.com:8080/block/#{@alert.id}/#{@person.first.to_i}"%> it and remove yourself from this alert forever.